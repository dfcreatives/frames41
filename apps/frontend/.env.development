# Dev API proxy target. Production is the default so the app works even when
# the local backend/database is unavailable. Set to http://localhost:3000
# when running the backend locally.
API_PROXY_TARGET=http://localhost:3000
